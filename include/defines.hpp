#ifndef DEFINES_H
#define DEFINES_H

constexpr int LINE_SIZE = 1024;
#define SKIP_LINE(no) if (no == nullptr) { puts("Skipping"); continue; }

constexpr int H = 0;
constexpr int S = 1;
constexpr int V = 2;

#endif // DEFINES_H
